description = 'CG2 PBX'
group = 'com.callgrove'
version = '2.0-SNAPSHOT'
apply plugin: 'war'


dependencies {
    providedCompile(
            'javax.servlet:javax.servlet-api:3.1.0',
            project(':obj'),
            project(':msg:aj')
    )
}
